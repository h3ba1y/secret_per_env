name: 'Build and Deploy to dev'

on:
  push:
    branches:
      - develop
      - master
      - qa

jobs:
  deploy-to-env:
    if: [[ "${{GITHUB_REF}}" == "refs/heads/develop" ]]
    uses: ./.github/workflows/test.yml
    secrets: inherit
    with:
      env: dev
    if: [[ "${{GITHUB_REF}}" == "refs/heads/master" ]]
    uses: ./.github/workflows/test.yml
    secrets: inherit
    with:
        env: prod
    if: [[ "${{GITHUB_REF}}" == "refs/heads/qa" ]]
    
    
