name: 'Build and Deploy to dev'

on:
  push:
    branches:
      - develop
      - master
      - qa

jobs:
  deploy-to-env:
    name: "deploy to environment"
    uses: ./.github/workflows/test.yml
    secrets: inherit
